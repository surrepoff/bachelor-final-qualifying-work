<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <ImageView
        android:id="@+id/itemPlaylistHorizontal_playlistImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:contentDescription="Image"
        android:src="@drawable/default_album"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/itemPlaylistHorizontal_playlistName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Playlist name"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/itemPlaylistHorizontal_playlistImage"
        app:layout_constraintStart_toStartOf="@id/itemPlaylistHorizontal_playlistImage"
        app:layout_constraintTop_toBottomOf="@id/itemPlaylistHorizontal_playlistImage" />

    <TextView
        android:id="@+id/itemPlaylistHorizontal_ownerName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Owner name"
        android:textAlignment="center"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/itemPlaylistHorizontal_playlistImage"
        app:layout_constraintStart_toStartOf="@id/itemPlaylistHorizontal_playlistImage"
        app:layout_constraintTop_toBottomOf="@id/itemPlaylistHorizontal_playlistName" />

</androidx.constraintlayout.widget.ConstraintLayout>